#!/bin/bash
# Pick staged file(s) with fzf and open in $EDITOR. Tab to multi-select.

files=$(git diff --staged --name-only | fzf --multi --preview 'git diff --staged --color=always -- {}')
[ -z "$files" ] && exit 0

echo "$files" | xargs -r "${EDITOR:-nvim}"
